TensorFlow.js integration

Add `@tensorflow/tfjs` as an npm dependency and use examples in `src/ai_samples/tfjs_sample.js`.

Example client install:

```bash
npm install @tensorflow/tfjs
```

Then include the sample in your frontend bundle.
